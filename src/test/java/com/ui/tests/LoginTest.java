package com.ui.tests;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

import com.utilities.BrowserUtility;

public class LoginTest {

	public static void main(String[] args) {

		WebDriver wd = new ChromeDriver();

		/*
		 * BrowserUtility browserUtility = new BrowserUtility(wd);
		 * browserUtility.navigateToApplication(
		 * "https://automationpractice.techwithjatin.com/login?back=my-account");
		 * 
		 * // wd.get("https://automationpractice.techwithjatin.com/");
		 * 
		 * // wd.manage().window().maximize(); browserUtility.maximizeWindow(); By
		 * signInLinkLocator = By.xpath("//a[contains(text(),'Sign')]"); // WebElement
		 * signInWebElement = wd.findElement(signInLinkLocator);
		 * 
		 * browserUtility.ClickOn(signInLinkLocator);
		 * 
		 * By emailTextBoxLocator = By.xpath("//input[@id='email']"); // WebElement
		 * emailAddressElement = wd.findElement(emailTextBoxLocator);
		 * 
		 * browserUtility.clearText(emailTextBoxLocator);
		 * browserUtility.enterText(emailTextBoxLocator, "neyef43651@azeriom.com"); //
		 * emailAddressElement.sendKeys("neyef43651@azeriom.com");
		 * 
		 * By passwordTextBoxLocator = By.xpath("//input[@id='passwd']"); // WebElement
		 * passwordWebElement = wd.findElement(passwordTextBoxLocator);
		 * 
		 * browserUtility.clearText(passwordTextBoxLocator);
		 * 
		 * browserUtility.enterText(passwordTextBoxLocator, "naman@2026");
		 * 
		 * // passwordWebElement.sendKeys("naman@2026");
		 * 
		 * By submitLoginbuttonLocator = By.xpath("//button[@id='SubmitLogin']");
		 * 
		 * 
		 * WebElement submitLoginbuttonWebElemnt =
		 * wd.findElement(submitLoginbuttonLocator); submitLoginbuttonWebElemnt.click();
		 * 
		 * 
		 * browserUtility.ClickOn(submitLoginbuttonLocator); wd.quit();
		 */
	}

}
