package com.ui.tests;

import static com.constants.Browser.*;

import org.testng.Assert;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

import static org.testng.Assert.assertEquals;
import static org.testng.annotations.Test.*;

import com.constants.Browser;
import com.ui.pages.HomePage;

public class LoginTest3 {
	/*
	 * HomePage homepg;
	 * 
	 * @BeforeMethod(description = "Load the homepage of the website") public void
	 * setUp() { homepg = new HomePage(CHROME);
	 * 
	 * }
	 * 
	 * @Test(description =
	 * "verifies with valid credentials user is able to login to the Application",
	 * groups = { "e2e", "sanity" }) public void loginTest() {
	 * 
	 * assertEquals(homepg.navigateToLoginpage().loginWithvalidCredentials(
	 * "neyef43651@azeriom.com", "naman@2026") .getUsername(), "Naman datte"); }
	 */
}
